"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.groupKeysMap = exports.groupDisabledKeysMap = void 0;
var groupKeysMap = function groupKeysMap(keys) {
  var map = new Map();
  keys.forEach(function (key, index) {
    map.set(key, index);
  });
  return map;
};
exports.groupKeysMap = groupKeysMap;
var groupDisabledKeysMap = function groupDisabledKeysMap(dataSource) {
  var map = new Map();
  dataSource.forEach(function (_ref, index) {
    var disabled = _ref.disabled,
      key = _ref.key;
    if (disabled) {
      map.set(key, index);
    }
  });
  return map;
};
exports.groupDisabledKeysMap = groupDisabledKeysMap;